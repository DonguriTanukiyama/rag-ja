# 測定 — 現場の帳票を正規化すると何が変わるか

日付: 2026-09-12（外部レビューの指摘を反映して改訂）
対象: `genba_norm.py` ＋ `rag_ja.py` の front-matter 取り込み

## この測定の位置づけ — **PoC（概念実証）**

**統計的な主張はしていない。**

- データは**自作サンプル1セット**（点検記録25行＋日報20件）
- 質問は**4問**
- **実物の帳票では試していない**

言えるのは「**この条件では、こう動いた**まで。
**定量評価は実データでの今後の課題。**

実物の点検記録は前職の社外秘で使えない。
そこで「実物がどう汚れているか」だけを写したサンプルを `samples/make_samples.py` で作った。
**汚し方の中身は、現場を見ていないと書けない。そこを読んでほしい。**

再現:

```
python samples/make_samples.py                                  # 汚れたサンプル
python genba_norm.py samples/raw -o samples/_norm                # 正規化（1点検=1文書）
python genba_norm.py samples/raw -o samples/_naive --naive       # 対照（正規化なし）
python genba_norm.py samples/raw -o samples/_norm_single --single-file
python rag_ja.py index samples/_norm
python rag_ja.py ask "2026年9月3日の点検で異常はありましたか" --where date=2026-09-03
python genba_norm.py --demo    # 自己確認。openpyxl があれば読み取りの突き合わせもする
```

---

## 1. 情報の抽出網羅率

**埋め込みにも LLM にも依存しない決定的な値**。ここがいちばん堅い。

| | 素朴に読む | 正規化後 |
|---|---|---|
| **日付が確定した行** | **12 / 25（48%）** | **25 / 25（100%）** |
| **点検欄として出てきたマス** | **89 / 100** | **100 / 100** |
| 部署の表記 | 4通り | **1通り** |
| 担当の表記 | 6通り | **2通り** |

日付の差13行は**結合セルを展開しないと日付が付かない行。**
1日に3回点検した日は、日付が先頭行にしか入っていない。

マスの差11は**すべて空欄**。素朴な実装に悪意はない。値の無いセルは書きようがないので消える。

読めた日付の形: `2026/9/1` `2026-09-02` `令和8年9月3日` `9/4` `R8.9.5` `26.9.6` `46266`（シリアル値）
**読めないものは捨てずに「日付が読めない（原文: …）」と書く**。でっち上げない。今回は0件。

---

## 2. 点検欄を6つに分ける — **点検漏れの検出**

点検欄は 25行 × 4項目 = 100マス。

| 分類 | マス | 意味 |
|---|---|---|
| 記入あり | **57** | 異常なし／特になし／OK／レ／✓／良 |
| 異常 | **11** | 「異常あり：掻き取り機に異音」「NG」 |
| **条件付き許容** | **8** | 要観察／経過観察／軽微**稼働は継続。異常ではない** |
| **対象外** | **6** | 計画停止中／休転／対象外**空欄でも未記入ではない** |
| **未記入** | **11** | 空欄。書いたのか書き忘れたのか分からない |
| **判別不能** | **7** | `-` `ー` `／` `N/A`。「該当なし」か「未チェック」か決められない |

**記入率 82%**（記入あり・異常・条件付き許容・対象外を、点検された記録として数えた）。

### これが何の役に立つか

**「今月の異常は11件」で終わらせない。**

未記入11 ＋ 判別不能7 = **18マスぶん、点検された記録が無い。**
異常が無かったのではなく**見ていない。**

現場では、これが**点検漏れ**であり、見逃した劣化がそのまま
**設備の停止（ダウンタイム）とヒヤリハット**につながる。
記入率82%という数字は「帳票がきれいか」の話ではなく、
**18マスぶん、異常を検知する機会が無かった**という話。

逆に**対象外6マスを点検漏れに数えてはいけない**。設備が止まっていれば点検しない。
そこを分けないと、改善すべき点検漏れの件数が実態より膨らみ、現場の信用を失う。

### 出力の形

```
- 加圧浮上槽 掻き取り: **未記入**（点検されたかどうか、原票からは分からない）
- 活性汚泥槽 色: **判別不能**（原文: -）— 「該当なし」か「未チェック」か決められない**異常という意味ではない**
- 加圧浮上槽 異音: **対象外** — 計画停止中（設備が稼働していないため点検しない。未記入ではない）
- 活性汚泥槽 発泡: **条件付き許容** — 要観察（発泡多め）（稼働は継続している。異常には数えない）
```

**決められないことを、決められないと書く。**
そして**その状態が何を意味しないのかまで書く**（判別不能は異常ではない、対象外は未記入ではない）。

---

## 3. 集計は、前処理で作って文書に書く

**RAG は検索であって集計ではない。**
「未記入は何件か」に答えさせたいなら**その一文がコーパスのどこかに存在しなければならない。**
埋め込みをいくら良くしても、書かれていない数は出てこない。

そこで `点検記録_2026-09_集計.md` を1本出している。
記入率・6分類の内訳・項目ごとの内訳・日付が読めなかった行の一覧が、そのまま文になっている。

---

## 4. 検索が当たるか

**LLM を挟まずに、検索だけを測る**。埋め込み bge-m3 / 上位5件。
生成を通すと、当たったのか当てずっぽうなのかが混ざる。

正解の置き場所:
- Q1「2026年9月3日の点検で異常はありましたか」→ 9/3 の点検記録
- Q2「点検欄が記入されていない項目は何件ありますか」→ 未記入11マスと書いてある箇所

| | Q1 の順位 | Q2 の順位 | 上位5件の日報混入 |
|---|---|---|---|
| 素朴 | 1位 ※ | **入らず** | 4〜5件 |
| 正規化・1ファイル | **入らず** | 1位 | 0〜2件 |
| **正規化・1点検＝1文書** | **3位** | **1位** | **0件** |
| **同上 ＋ `--where date=2026-09-03`** | **1位** | — | **0件** |

※ **素朴の1位は勝ちではない**。25行の表がまるごと1断片なので、月ぶん全部が当たっただけ。
9/3を絞れているわけではない（上位5件のうち4件は日報）。
チャンクが大きすぎて全部ヒットするのは、精度が高いのではなく**偽陽性**

**この4問では、1点検＝1文書が勝った**。→ 既定をそれに変えた（1ファイル版は `--single-file` で残した）。

### メタデータで絞ると何が変わるか

`--where date=2026-09-03` を付けた場合**上位に出たのは該当2文書だけ**（点検記録1件＋日報1件）。
**無関係な日が1件も混ざらない。**

「9月3日の」という条件は**意味の近さではなく完全一致で絞るべきもの。**
ベクトル検索だけに任せると、他の日の点検記録が距離0.74〜0.78で並び、
正解（0.7747）がその中に埋もれる。

実装: `genba_norm.py` が各文書の先頭に front-matter を出し、
`rag_ja.py` の索引がそれを metadata に入れる。

```
---
doc_type: inspection
date: 2026-09-03
month: 2026-09
equipment: 加圧浮上槽,活性汚泥槽
person: 山田太郎
dept: 第1製造部
source: 点検記録_2026-09.xlsx
row: 6
---
```

**front-matter 自体は索引に入れない**（`---` の塊が断片に混ざると検索の邪魔になる）。

### 索引の粒度

| | 文書 | 断片 |
|---|---|---|
| 素朴 | 21 | 23 |
| 正規化・1ファイル | 22 | 42 |
| 正規化・1点検＝1文書 | **46** | **47** |

1点検＝1文書だと、断片と文書がほぼ1対1。
引用が `点検記録_2026-09/2026-09-03_r06.md` まで絞れるので、
**どの日のどの行を見て言っているのかが読む人に分かる。**

---

## 5. 生成まで通した場合

生成 qwen2.5:7b / 埋め込み bge-m3 / 上位5件（温度0）。

| | 質問 | 答え | 正解 | |
|---|---|---|---|---|
| Q2 | 点検欄が記入されていない項目は何件ありますか | **「11件」**（根拠: 集計文書） | 11 | ○ |
| Q3 | 2026年9月の点検で異常は何件ありましたか | **「11件」**（根拠: 集計文書） | 11 | ○ |
| Q4 | 点検されていない項目は何件ですか。設備が停止していた分は含めないでください | **「2件」** | 18 | **✕** |
| Q4' | 同じ質問に `--where doc_type=summary` を付けた | **「18件です。ただし、設備が停止していた分（対象外の6件）は含めておりません」** | 18 | **○** |

### Q3 — レビューの懸念は起きなかった

外部レビューで「**LLMが『判別不能』を『異常』と誤認しないよう、プロンプト側の強い制約とセットにする必要がある**
と指摘されていた**測った結果、起きなかった。**

異常11件と正しく答え**判別不能7マスも条件付き許容8マスも混ざらなかった。**
`rag_ja.py` の `PROMPT` は1文字も変えていない。

効いたのは**出力の文言のほう**だと考えている。

```
- 活性汚泥槽 色: **判別不能**（原文: -）— 「該当なし」か「未チェック」か決められない**異常という意味ではない**
```

**その状態が何を意味しないのかまで書いてある**。本体を触る前に、書き方で解けた。

### Q4 — 誤答した。そして原因が切り分けられた

最初の実行は **「2件」。正解は18**（未記入11＋判別不能7。対象外6は除く）。

**ただし検索は当たっていた**。上位5件に集計文書の断片が2つとも入っており、
`[2]` には「**合わせて 18 マス**「**対象外 6 マスは未記入ではない**がそのまま含まれていた。
**根拠は届いていたのに、生成が数えられなかった。**

原因の候補は3つあった。

| | 検証 |
|---|---|
| モデルの限界か | gemma3:12b で再試行 → **VRAM で詰まって取得できず**（8.1GB＋bge-m3 が載らない） |
| コーパスの書き方か | 集計文書には正解の一文が既にある（上記） |
| **ノイズか** | **`--where doc_type=summary` を付けたら、同じ 7b が完全に正答した** |

**答えはノイズ。**
同じモデル・同じコーパス・同じ質問で**違いはフィルタだけ。**
上位5件のうち3件が点検記録の個票で、その中の「未記入」の行が混ざると、
モデルはそちらを数えようとして小さい数を出す。

**集計を聞く質問は、集計文書だけに絞る。**
「9月3日の」は `date` で絞る**どちらも、意味の近さではなく完全一致で絞るべき条件。**

### まとめると

**フィルタ無しで 4問中3問。フィルタを使えば 4問中4問。**
ただし**フィルタを付けるかどうかを今は人間が決めている。**
質問から自動で絞り込み条件を作る仕組みは無い**そこは未実装。**

## 6. 内蔵の読み取り vs openpyxl

xlsx は **openpyxl があれば使い、無ければ内蔵の読み取り（標準ライブラリのみ）に落ちる。**
どちらを使ったかは必ず表示する。`rag_ja.py` の埋め込みと同じ方針で**黙って弱いほうに落ちない。**

```
xlsx の読み取り: openpyxl 3.1.5
xlsx の読み取り: 内蔵（標準ライブラリのみ）
  注意: 1904年日付システム / Strict ネームスペース / 旧.xls / 複数シート に未対応。
        `pip install openpyxl` で差し替えられる
```

**`--demo` が両者を突き合わせる。**

```
突き合わせ: 内蔵 == openpyxl 3.1.5（結合14箇所・全マス一致）
```

自前で xlsx を解くのは方言に弱い。**弱いことは認めたうえで、
「手元のファイルでは openpyxl と同じ答えを返す」ことは示せる。**

---

## 7. 測っていないこと（はっきりさせておく）

- **質問は4問しかない**。統計的な主張はできない。§0 のとおり PoC
- サンプルは**自作**実物では試していない。汚れの種類は写したが、量と分布は実物と違う
- **ファイル単位の Recall@k は測っていない**。コーパスが46文書しかなく、この変更の効果が出る指標ではない。
  代わりに §1 の**抽出網羅率**（決定的な値）と §4 の**正解の順位**で出している
- **RAGAs 等による Faithfulness は測っていない**。LLM を審判にする指標はブレるので、
  決定的な数字を固めてから足す
- **数万文書の規模で検証していない**。1点検＝1文書は文書数が行数ぶん増える。
  メタデータ・フィルタを入れたのはその備えだが、規模での確認はしていない
- 表記ゆれの寄せ表（`ALIASES`）は現場ごとに違う**そのまま他社に使えるものではない**
- `parse_date` は **1904年日付システムに未対応**。`12.3.4` のようなバージョン番号を
  2012-03-04 と読む**既知の誤検知**がある（日付列と日報の1行目からしか呼んでいないので実害は出ていない）

---

## 8. 作っていて落とした誤り（記録）

### ① `classify()` の判定順が逆だった

**「異常なし」が「異常」で始まる**ため、接頭辞判定に先に当たって**異常として数えられていた。**
正常が全部 異常 に化ける。集計が逆さまになる種類の誤り。
自己確認（`--demo`）の1行目で落ちて気づいた。
→ **正常の語を先に見る**。接頭辞判定はその後にしか置けない。

### ② 別名の置換が、既に正しい形にも当たっていた

日報の本文で「加圧浮上槽」が **「加圧加圧浮上槽槽」** になっていた。
別名表が `加圧浮上槽 ← [浮上槽, 加圧浮上]` なので、正しい形の中身に置換が二度当たっていた。
**`--demo` では出なかった**（単語1つを渡す試験しか書いていなかった）。
**出力の本文を目で見て気づいた。**
→ 代表名と別名を退避してからまとめて戻す方式に変更。

### ③ 「要観察」を異常に数えていた／「計画停止中」を未記入に数えていた

**外部レビューの指摘で判明。**
現場では「一部劣化だが稼働継続」は異常ではない。異常件数が実態より膨らむ。
また設備が止まっていれば点検しない。空欄でも未記入ではない。
→ 4分類から**6分類**にした**集計の数字が変わったので、測り直している。**

**①②は自分で見つけた。③は見つけられなかった。**
①は自己確認で、②は出力を目で見て、③は**外から指摘されて**
**どれも「作った本人の頭の中」だけでは出てこない。**
